// Others
/*
 A: 在GitBub端
 1. 進入Your profile頁面
 2. 選擇Repositories標籤頁
 3. 按下"New"新增Repositories
 
 B: 在Local端
 1. $ git init, 建一個空的git
 2. $ git remote add origin URL, remote遠端
 3. $ git status, 檢查更新的檔案
 4. $ git add, Add要修改上傳的檔案
 5. $ git commit, 將這次的修改存成一個commit並且增加說明
 6. $ git push origin master, 將commit上傳
 */
